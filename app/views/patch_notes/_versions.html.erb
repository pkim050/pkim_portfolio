List of all versions:
<ul>
  <% Dir.children('./app/views/patch_notes/').sort { |a, b| b <=> a }.each do |patch_note| %>
    <% if patch_note.split('.html.erb').first.match(/v[0-9]+.[0-9]+.[0-9]+/) %>
      <li><a href="/patch_notes/<%= patch_note.split('.html.erb').first.gsub('_', '.') %>"><%= patch_note.split('.html.erb').first.gsub('_', '.') %></a></li>
    <% end %>
  <% end %>
</ul>
